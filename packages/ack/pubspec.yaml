name: ack
description: Schema validation and bidirectional codecs for Dart and Flutter
version: 1.5.0
repository: https://github.com/conceptadev/ack
issue_tracker: https://github.com/conceptadev/ack/issues
homepage: https://concepta.dev/ack

resolution: workspace
topics:
  - validation
  - schema
  - json-schema
  - codec
  - serialization

environment:
  sdk: '>=3.9.0 <4.0.0'
dependencies:
  meta: ^1.15.0
  collection: ^1.18.0

dev_dependencies:
  test: ^1.25.15
  coverage: ^1.11.1
  dart_code_metrics_presets: ^2.22.0
  lints: ^5.0.0
  path: ^1.9.0

# Melos workspace configuration - managed by workspace
